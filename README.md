# Apache Airflow

## Application 실행
./gradle clean build bootRun
cf) http://localhost:8080/api/swagger-ui/index.html

## Airflow 실행
cd airflow 
docker compose up -docker

## Airflow UI 실행
htp://localhost:8808
